
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop, feature/*]
  pull_request:
    branches: [main, develop]

jobs:
  # ============================================
  # Backend: Django DRF + JWT API Tests
  # ============================================
  backend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: backend

      - name: Run flake8
        run: flake8 . --max-line-length=79 --exclude=venv,migrations
        working-directory: backend

      - name: Run pytest
        run: pytest --maxfail=1 --disable-warnings -v
        working-directory: backend
        env:
          SECRET_KEY: test-secret-key-for-ci

  # ============================================
  # Frontend: Next.js + React + Tailwind
  # ============================================
  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run tests
        run: |
          cd frontend
          npm test -- --watchAll=false --passWithNoTests

      - name: Build check
        run: |
          cd frontend
          npm run build

  # ============================================
  # Deploy: Frontend (Vercel) + Backend (Render/PythonAnywhere)
  # ============================================
  deploy-frontend:
    needs: [backend, frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Deploy Frontend to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./frontend

  deploy-backend:
    needs: [backend, frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      # Option A: Deploy to Render (recommended for MVP)
      - name: Trigger Render Deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"

      # Option B: Deploy to PythonAnywhere (uncomment if using PA)
      # - name: Deploy to PythonAnywhere
      #   env:
      #     PA_USERNAME: ${{ secrets.PA_USERNAME }}
      #     PA_API_TOKEN: ${{ secrets.PA_API_TOKEN }}
      #   run: |
      #     pip install pythonanywhere
      #     pa_autoconfigure_django.py \
      #       --python=3.12 \
      #       --nuke \
      #       https://github.com/${{ github.repository }}.git \
